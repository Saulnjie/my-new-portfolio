import styled from "styled-components";
const H2 = styled.h2`
  font-family: "Raleway";
  font-size: var(--font-size-lg);
  color: var(--color-white);
  /* margin-top: 50px; are we seaing the project tag still on all screens? */
`;

export default H2;
